mybatis:
  mapper-locations: classpath:mapper/*.xml

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: user
    password: password
  webflux:
    multipart:
      max-parts: -1
      file-storage-directory: classpath:static/upload

logging:
  level:
    mapper:
      mybatis: debug



