<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.anamensis.server.mapper.OTPMapper">
    <select id="selectByUserId" resultType="OTP">
        SELECT a.*
          FROM otp a
          JOIN user b
            ON a.user_pk = b.id
         WHERE b.user_id = #{userId}
           AND a.is_use  = 1
    </select>

    <select id="selectByUserPk" resultType="OTP">
        SELECT *
          FROM otp
         WHERE user_pk = #{userPk}
           AND is_use  = 1
    </select>

    <select id="selectByHash" resultType="OTP">
        SELECT *
          FROM otp
         WHERE hash = #{hash}
    </select>

    <insert id="insert" parameterType="OTP">
        INSERT INTO otp (user_pk, hash, create_at)
        VALUES (#{userPk}, #{hash}, #{createAt})
    </insert>

    <update id="updateIsUse" parameterType="OTP">
        UPDATE otp
           SET is_use = #{isUse}
         WHERE hash   = #{hash}
    </update>
</mapper>