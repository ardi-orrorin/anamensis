<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.anamensis.server.mapper.SmtpPushHistoryMapper">
    <select id="findByUserPk" resultType="com.anamensis.server.resultMap.SmtpPushHistoryResultMap$ListSmtpPushHistory">
        SELECT id, subject, create_at, status, message
          FROM smtp_push_history
         WHERE user_pk = #{userPk}
         ORDER BY create_at DESC
    </select>
    <select id="countByUserPk" resultType="long">
        SELECT COUNT(*)
          FROM smtp_push_history
         WHERE user_pk = #{userPk}
    </select>
    <select id="findById" resultType="SmtpPushHistory">
        SELECT *
          FROM smtp_push_history
         WHERE id = #{id}
    </select>

</mapper>